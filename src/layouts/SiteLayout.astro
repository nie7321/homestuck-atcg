---
import Chrome from './Chrome.astro';

interface Props {
    title?: string;
}

const { title } = Astro.props.frontmatter || Astro.props;
---
<Chrome title={title}>
    <main class="max-w-5xl mt-20 mb-20 py-6 px-4 mx-auto bg-white">
        <slot />
    </main>
</Chrome>